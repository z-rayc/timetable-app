import 'package:flutter/material.dart';

class TempTimetableScreen extends StatelessWidget {
  const TempTimetableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Timetable'),
      ),
    );
  }
}
